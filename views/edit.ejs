<% include partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">Edit medicine data</div>
    <form class="ui form" action="/medicine/<%=emed._id%>?_method=PUT" method="POST">
        <div class="field">
            <label>Title</label>
            <input type="text" value="<%= emed.name %>" name="name">
        </div>
        <div class="field">
            <label>description</label>
            <textarea name="description"><%= emed.description.substring(0,100); %></textarea>
        </div>
        <button class="ui big violet basic button">Update</button>
    </form>
</div>

<% include partials/footer %>